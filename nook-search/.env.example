# AnySearch API Key 配置示例
# =================================
# nook-search 本身不读取 .env，这个文件是给它依赖的 AnySearch CLI 用的。
# 把下面这行复制到 AnySearch skill 目录下的 .env 文件里（不是本目录）。
#
# 可选但推荐配置：不配置 key 也能用（匿名访问，速率限制较低）。
# 获取方式：访问 https://anysearch.com/console/api-keys 免费注册
#
# 优先级：--api_key 命令行参数 > .env 文件 > 系统环境变量 > 匿名访问

ANYSEARCH_API_KEY=<YOUR_API_KEY>
